
const endpoints = {
    "users": {
        "method": "GET",
        "url": "/users"
    },
    "user_detail": {
        "method": "GET",
        "url": "/user/{id}"
    },
}

export default endpoints;
